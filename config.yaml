# ACE server configuration

server:
  host: "0.0.0.0"
  port: 8888

llm:
  provider: "ollama"
  model: "qwen3:14b"
  context_messages: 50
  # host: "http://localhost:11434"  # uncomment to override default
