/**
 * WebSocket connection state store.
 *
 * Reactive Svelte store tracking connection status:
 * 'connected' | 'disconnected' | 'reconnecting'.
 * Components subscribe to this for UI indicators.
 */
